Make English the default README and publish both packages through trusted publishing.

- Add a Chinese README with links between both languages.
- Update npm and PyPI package descriptions to use the English README.
- Install directly from npm with `npx --yes wezterm-codex-status-line@latest`.
- Publish npm and PyPI packages only from validated version tags.

Update an existing installation:

```sh
npx --yes wezterm-codex-status-line@latest update
# Or:
uvx --refresh wezterm-codex-status-line update
```
